import { KoaController } from "../../../utils/KoaController";

export const saveBookmarks: KoaController = async (ctx) => {
  // ToDo: Implement
  ctx.status = 200;
  ctx.body = {
    error: 0,
  };
};
