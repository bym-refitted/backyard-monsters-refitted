/**
 * Enum representing the different types of yard types.
 * @enum {number}
 */
export enum EnumYardType {
  MAIN_YARD = 0,
  OUTPOST = 1,
  INFERNO_YARD = 2,
  INFERNO_OUTPOST = 3,
  EMPTY = 100,
  PLAYER = 101,
  RESOURCE = 102,
  STRONGHOLD = 103,
  FORTIFICATION = 104,
  BORDER = 127,
}
