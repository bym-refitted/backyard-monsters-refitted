/**
 * Enum representing the executing environment
 * @enum {string}
 */
export enum Env {
  LOCAL = "local",
  PROD = "production",
}

export enum Auth {
  ENABLED = "enabled",
  DISABLED = "disabled",
}