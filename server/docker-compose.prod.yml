services:
  web:
    ports:
      - "80:3001"
      - "${CHAT_WS_PORT:-3010}:3010"
      - "843:843"
    environment:
      - ENV=production

  # Don't expose db or redis ports to the host in production
  db:
    ports: []

  redis:
    ports: []
